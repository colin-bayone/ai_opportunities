# Neha Follow-Up Email v4

---

Really enjoyed today's call. Getting into the details of the Framework Model vs Freehand SQL workflow, and hearing how you want the DataStage output to be YAML configs rather than code, gave us exactly what we need to build something useful.

Here's what we're planning for the demo:
• A Cognos connector that pulls report metadata and SQL directly, so no more manual XML exports from Report Studio
• SQL translation from SQL Server to Databricks
• If time allows, DataStage conversion that outputs YAML configs ready to commit to your repo

To get started, we need a few things:
1. A Cognos report XML from Finance. Vlad can help pull this.
2. The target Databricks schema, catalog and table structure, so we can wire up the remapping.
3. A sample YAML config from your existing framework so we match your output format. Sergei would know best here.
4. A DataStage job if one's easy to grab. Maher mentioned having one.

No pressure on timing, but the sooner we get these, the sooner we can schedule the demo and show you how it works with your own stuff.

Let me know if anything's unclear about what to export. Happy to hop on a quick call.

Best,
Neha
