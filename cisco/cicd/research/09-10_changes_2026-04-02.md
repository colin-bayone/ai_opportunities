# 09-10 Bridge: What Changed

**From:** Set 09 (Internal team meeting, 2026-03-30)
**To:** Set 10 (CI/CD Track Sync Up with Srinivas's team, 2026-04-02/03)
**Gap:** 3 days

---

## Hypotheses Validated

1. **Srinivas moves fast.** Every prior set described this. Set 10 is the first time we see it in action: concrete tasks assigned, recurring meetings established, team structure defined, outcomes framework laid out — all in a 45-minute call.

2. **MCP/agentic architecture is central.** Set 06 introduced the "two hats" (current need + agentic future). Set 10 operationalizes it: every data source needs an MCP, tool calls must serve both user-driven and agentic modes.

3. **Access remains the bottleneck.** Set 07 tracked identical access lists across two months. Set 10: GitHub is STILL blocked (Duo MFA issue demonstrated live). DeepSight access still pending.

4. **Cisco's build infrastructure is fragmented across teams.** Set 06 noted different teams own different systems. Set 10 confirms: Justin owns build data (MySQL, NFS), Anupma's DevEx team owns CAT databases (Cassandra), Divakar owns Jenkins/ADS. Cross-org negotiation required.

## Hypotheses Invalidated

1. **Rui handoff is not the starting point.** Set 06: Srinivas said BayOne would pick up Rui's CI/CD app on DeepSight. Set 10: Rui is not mentioned at all. The starting point is instead the WebEx scraper + Justin's build logs. The engagement is beginning from user pain point analysis, not from an existing app handoff.

## New Information Not in Prior Sets

1. **Anupma Sehgal (DevEx lead).** Completely new. Co-owns the CI pipeline. Controls CAT databases. Has reservations about exposing data. This is a new organizational boundary and potential blocker.

2. **Justin Joseph (build infrastructure).** Completely new. Has MySQL databases, NFS logs, existing MCP with basic capabilities, AI-based fix suggestions. Key technical counterpart for BayOne.

3. **Naga** has existing WebEx plugins (chat scraper, recording transcriber). May already have committed code.

4. **Call graph from CLAM/LAM system.** Two approaches: baseline from nightly build, dynamic for new code. Distinct from CRFT (runtime instrumentation on switches).

5. **Inference cost is a hard constraint.** 4X increase already. Srinivas explicitly warns against naive approaches (throwing entire logs at LLMs).

6. **Build logs on NFS with 3-5 day retention.** Must be processed in near-real-time or copied out before they expire.

7. **First concrete tasks assigned.** WebEx scraper (user pain point analysis), build log understanding (Justin meetings). These are actual deliverables, not discovery.

8. **Srinivas wants BayOne team profiles/resumes.** Wants to understand strengths, assign work directly.

9. **Twice-weekly recurring meetings established** with this team.

10. **Srinivas will assign BayOne to other DeepSight work if CI/CD is blocked.** "I don't want them to be idle."

## Questions Answered from Prior Sets

| Prior Question | Answer from Set 10 |
|---------------|-------------------|
| What are the first concrete tasks? | WebEx scraper + build log analysis with Justin |
| Is Airflow available at Cisco? | Yes, existing DAGs, use existing infrastructure |
| What databases exist? | MySQL (build), Cassandra (CAT/DevEx), NFS (logs) — expanded from Set 06 |
| Who is the Airflow SME? | Not identified, but Airflow infrastructure confirmed available |

## Questions Still Open

- Rui's CI/CD app status (not mentioned — is the handoff still happening?)
- Anupma's willingness to expose DevEx databases
- Naga's existing code availability
- Airflow SME identity
- GitHub MFA resolution timeline
- Full call graph / LAM system access
