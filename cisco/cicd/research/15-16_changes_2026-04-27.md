# 15-16 Bridge: Friday Inflection to Monday Cisco-side Sync

**From:** Main Set 15 (Friday afternoon Srinivas sync, 2026-04-24)
**To:** Main Set 16 (Monday Cisco-side Srinivas sync, 2026-04-27)
**Written:** 2026-04-27

---

## Context

Main Set 15 (Apr 24) was the consolidation Friday sync that defined the May 1 deployment target precisely, unblocked the NX repository access through Srinivas's personal action, finalized the deployment form, and established the Monday cadence. Main Set 16 is the first Monday execution under that cadence. Colin walked Srinivas through the structured weekly status one-pager (`weekly_status_2026-04-27_v3_table.md`) item by item. The meeting both resolved or progressed every blocker on that document and surfaced two material new directions that were not on the Friday horizon.

Anand joined this meeting and intervened immediately on the ADS blocker, which is the first time in the engagement chain that Anand has driven a working-meeting-level operational resolution.

---

## Hypotheses Validated

### 1. Monday cadence as defined would land cleanly

Set 15 defined: GitHub markdown plus Mermaid as the deliverable format, Monday meetings tracked in CI/CD repo issues, weekly status one-pager as Srinivas's preferred summary surface. Set 16 confirms: Colin presented the structured one-pager, Srinivas asked for two specific edits (top-level item with sub-bullets format, plus an "accomplished" section), accepted the rest, and used it to drive the meeting. The deliverable format is now operationalized.

### 2. NX repository access path would be effective

Set 15: Srinivas personally added BayOne user IDs to lead-only group, replacing IT provisioning. Set 16: Anupma's CAT MCP resolution explicitly leveraged the NX repo access ("Now that you have access to the NX GitHub, right? So you should be able to go there"). The access path Srinivas defined Friday is functional Monday.

### 3. Anupma trust trajectory would continue

Set 13: upgrade from "guarded" to "collegial." Set 16: full operational engagement-partner posture. She owned three of the meeting's most consequential outcomes (CAT MCP PR-mapping resolution, bot deployment form ownership, generic-user-ID pushback that Srinivas accepted). Highest-engagement Anupma session in the engagement to date.

---

## Hypotheses Invalidated, Reframed, or Underdelivered

### 1. ADS blocker reframed and resolved through Anand and Divakar, not Mahaveer

Set 15 reading: ADS provisioning was tracked through Mahaveer with permanent ADS resource constraints disclosed by Srinivas Friday. The Set 16 prep call (Team Set 16) further reframed it as a Cisco supply-side problem rather than a Mahaveer-execution problem. Set 16 Monday meeting: Anand cut Colin off mid-agenda to drive the conversation directly with Divakar. Mahaveer was not in the resolution path. Divakar committed to retire 4-5 underused VMs and procure 2 new 16-core / 32GB ADS machines, with one existing CN-SJC-STANDALONE bundle machine left immediately available. Today/tomorrow timeline. **Blocker 1 from the prep deliverable resolved.** This is a faster and cleaner resolution than Friday's framing predicted.

### 2. Static FAQ source materially pivoted

Set 15: BayOne would build the static FAQ from chat-history pattern analysis. Set 16: Srinivas redirected the static-FAQ source to the existing NX-OS wiki, which Anupma confirmed already exists with full PR workflows, FAQs, and startup guide documentation, but is ignored by engineers because chat is lower-friction. Colin pushed back ("we just keep on pivoting") and Srinivas partially apologized. Colin's chat-to-wiki feedback loop proposal accepted. Outcome: the static-FAQ workstream's data source has changed.

Additionally, Colin had been interpreting "static" as point-in-time-frozen-data. Srinivas re-defined "static" as no-DB-lookup-required versus dynamic as needs-DB-lookup. Colin acknowledged the prior misread. This semantic correction matters for everything downstream.

Anupma added a last-6-months data slice constraint with a toggle in the dashboard.

### 3. WebEx LLM access reality was more nuanced than the prep call suggested

Team Set 16 (prep call): Colin announced the "be annoying" cadence and rehearsed the four blockers framed in part around the assumption that WebEx bot would need LLM credentials separately. Set 16 (this meeting) revealed the actual current state: Srinivas hedged ("minus six months knowledge, backdated") on whether WebEx bots have LLM access; Colin confirmed in the meeting that WebEx now does support agentic apps with LLM integration. The clean architectural picture is that LLM access lives on the CICD app side via DeepSight (using user-registered credentials), not on the WebEx bot side. The bot is the user-facing surface; the CICD app is the LLM-bearing back end. This is a clearer architectural picture than the prep call carried.

### 4. WebEx-bot-as-proxy long-term direction emerged

Not in the prep deliverable. Srinivas described the long-term vision: CICD app on each user's own AD server is the future single-pane-of-glass for engineers; WebEx bot becomes a proxy/middleman to that app for on-the-go phone access. Per-user LLM credentials. Two teams running parallel experiments on the proxy pattern; first to crack it wins. Colin flagged Cloud Code's (Claude Code's) "remote control" feature as analog at BayOne. New research direction for BayOne this week.

### 5. PR Apollo and Builder Triaging surface as scope-overlap concerns

Not in the prep deliverable. Divakar revealed that Justin already has a "PR Apollo" GitHub-event MongoDB deployed last week, and that the Builder Triaging tool he developed is already in production use by client team and kernel team (800 features through it for clang migration). Srinivas was unaware: "this information is new to me also." Divakar politely jabbed Srinivas about missing the meeting series where this was discussed. Material scope-overlap concern: BayOne's planned dynamic answer path may overlap with existing Cisco infrastructure. Concrete next step: Justin to create read-only generic user ID for the team to access the MongoDB. Imran (Cisco engineer; last name unknown) was named as the person who raised Builder Triaging.

### 6. CAT MCP PR-mapping gap closed by data-already-existing, not by building a mapping

Team Set 16 prep call: Srikar's Friday research found 4 tools on the CAT MCP and a PR-to-CAT ID mapping gap; Colin walked into the meeting expecting BayOne to either request CAT MCP functionality extension or build a workaround mapping table. Set 16: Anupma resolved this by walking through the cache request structure live: the CAT request data already contains the full PR mapping plus rich metadata (branch, submitter, bug ID, SHA, sub-initiator, all checks). The mapping table BayOne thought it would have to build does not need to be built. **Blocker 4 resolved.**

The technical lesson: Srikar's analysis of the MCP tool surface was correct, but the underlying data is accessible via the cache request that fires on each PR check. There is a distinction between MCP-tools-as-exposed and MCP-underlying-data-via-PR-checks. Worth documenting.

### 7. Bot deployment identity resolved at the identity level; compliance criteria still pending

Team Set 16 prep call: Bot deployment identity and the bot compliance email were two separate items (Blockers 3 and 4 in some readings). Set 16: identity is resolved. Use existing DSA Atlas (also referred to as DSR Class) generic user ID. Anupma fills the form on BayOne's behalf using Colin's prior submission as template. Compliance criteria for the bot itself remains pending; the email Cisco IT sent flagging non-compliance did not include the criteria, and that thread is still open. Resubmission expected Wednesday once criteria are in hand.

### 8. Skills repository destination confirmed

Team Set 16 prep call: Saurav's framing endorsed by Colin, planned to present to Srinivas. Set 16: Srinivas confirmed in identical language. "For now keep it here, and whenever you see okay some skills are ready. Then it could be deployed by anyone. Then put it to the SME KB." Closes the open item from Main Set 15.

### 9. LLM access path clarified

Set 15 left this with Circuit API as the interim approach with a planned switch when production LLM credentials were in place. Set 16: Srinivas described the post-ADS sequence: ADS provisioned → build CICD app → deploy on ADS server → register credentials on the DeepSight website (already done) → DeepSight handles `.env` automatically → Copilot, Codex, Circuit API, and Cursor available as model options. The `.env.example` ownership question Colin raised was resolved: DeepSight handles it automatically. Other Cisco team members are committing a user guide and the Jenkins pipeline to the CICD repo today/tomorrow. **Blocker 2 resolved with a clear sequence.**

---

## What we got right going in

- Predicted Anand would matter at the executive layer. He showed up and drove the ADS resolution himself.
- Predicted Anupma would deliver on the CAT MCP technical question. She did, and more.
- Predicted Saurav's skills repository framing would land with Srinivas. Confirmed verbatim.
- Predicted the four blockers would be raised effectively in sequence. They were, and four of five resolved or progressed in the meeting.
- Predicted the GitHub issue tracking infrastructure would operate against the structured one-pager. The one-pager drove the meeting.

## What we got wrong going in

- Assumed Mahaveer would be central to the ADS resolution. He was not in the path; Anand and Divakar drove it directly.
- Assumed WebEx bot needed independent LLM credentials. The architecture is cleaner than that: LLM is on the CICD app side via DeepSight; WebEx bot is the user-facing surface. The agentic-app capability on WebEx (which Colin confirmed live) was also more current than the team had been operating on.
- Assumed BayOne would have to build the CAT-to-PR mapping table. The data is already in the cache request structure.
- Did not anticipate the static FAQ source would pivot to the wiki. Did not anticipate Justin's PR Apollo / Builder Triaging would surface as a scope-overlap concern.
- Read the bot compliance email primarily as a deployment-ID problem. The deployment-ID part is resolved; the actual compliance criteria for the bot itself remain opaque and outside BayOne's control.

## Questions opened

1. **What are the WebEx bot compliance criteria?** Cisco IT flagged Saurav's bot as non-compliant on Apr 27 morning but did not provide criteria. Anupma will fill the form for resubmission under DSA Atlas, but unless criteria are surfaced, resubmission is a best-guess exercise.
2. **Does PR Apollo or Justin's MongoDB cover BayOne's planned dynamic answer path?** If yes, BayOne's CAT MCP integration may shift from "build a parallel pipeline" to "build on top of what Justin already has."
3. **Is the Builder Triaging tool's functionality something BayOne should integrate with rather than reinvent?** Same question as above for build-failure analysis.
4. **What is the Basel MCP timeline?** Justin to find out today.
5. **How does the WebEx-as-proxy architecture map to BayOne's deliverables?** Srinivas framed it as "while you are studying the web accelerator." Is this a research deliverable, a Friday deliverable, or a future-quarter deliverable?
6. **When does the CICD repo receive the user guide and Jenkins pipeline commits?** Srinivas committed his other team to deliver "today, tomorrow or whenever." BayOne's CICD app deployment depends on these landing.
7. **Will the wiki-as-source-of-truth approach scale?** Anupma's observation that engineers ignore the wiki is the underlying problem. The chat-to-wiki feedback loop addresses the gap but the wiki only has authority if engineers start treating it as authoritative.

## Bridge to next set

Main Set 17 will be either the Wednesday Apr 29 sync (the next regular Cisco-side cadence point) or an interim event triggered by the bot compliance criteria, the CICD app deployment landing on ADS, or unblocks from this meeting. Watch for: ADS provisioning completion, user guide and Jenkins pipeline commits from Srinivas's other team, bot compliance criteria from Cisco IT, Basel MCP timeline from Justin, and the dashboard-link plus static-vs-dynamic intersection analysis Colin owes Srinivas this week.

The action items for the BayOne team's GitHub issue tracker (per the prep call's announced cadence) are captured in `16_meeting_action_items_and_week_assignments_2026-04-27.md`.
